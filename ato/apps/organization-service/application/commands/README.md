# Commands

Contains Pydantic request models for organization-service application commands. Commands describe requested state changes and carry audit context, but they do not execute business logic or depend on infrastructure.
