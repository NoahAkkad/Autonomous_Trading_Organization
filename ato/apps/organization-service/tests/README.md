# Tests

Contains institutional acceptance tests for the organization-service Sprint 1 bootstrap lifecycle.

The suite validates event creation, event store integrity, projection building, read model integrity, state rebuild, and audit traceability. Tests are fully automated and contain no skipped or xfail cases.
